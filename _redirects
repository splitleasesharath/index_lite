# Redirects for Cloudflare Pages

# Redirect www to non-www
https://www.split.lease/* https://split.lease/:splat 301

# Force HTTPS
http://split.lease/* https://split.lease/:splat 301

# Note: Bubble app is now on subdomain app.split.lease
# No proxy needed - it's handled via DNS A records